# cmudb

This project is intended to be, roughly, a Rust implementation of the CMU DB bustub system. While not everything is/will be the same, the general idea is to implement the assignments in Rust, as well as any other components that are required.
